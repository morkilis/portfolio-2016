/**
 * global configuration
 */
'use strict'
var M = window.M || {}

;(function(global) {
    for (var k in seajs) {
        M[k] = seajs[k]
    }

    M.app = {}
    M.config = M.pluginSDK.config

    delete global.seajs
    delete M.debug
    delete M.pluginSDK.config
})(window)
