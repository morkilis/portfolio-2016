define(function(require, exports, module) {
    'use strict'

    var DetailModel = require('/js/models/detail'),
        Backbone = require('Backbone'),
        API = M.config.API

    // merchant all deals model
    var AllDealsModel = Backbone.Model.extend({
        urlRoot: function() {
            return ( API + 'deal/search/substore/' + DetailModel.get('bizloginid') )
        }
    })

    module.exports = new AllDealsModel
})
